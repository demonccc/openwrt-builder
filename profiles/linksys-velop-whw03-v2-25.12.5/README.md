# Linksys Velop WHW03 v2 — OpenWrt 25.12.5

This profile builds the Linksys Velop WHW03 v2 on the exact OpenWrt 25.12.5 release using the official ImageBuilder.

The public profile ID carries the device and exact OpenWrt version. `METHOD=imagebuilder` remains an implementation detail in `settings` rather than part of the profile name.
