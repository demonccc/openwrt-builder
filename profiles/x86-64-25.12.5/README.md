# Generic x86/64 — OpenWrt 25.12.5

This profile builds a generic x86/64 firmware from the exact OpenWrt `v25.12.5` source using the `selective-source` mode and an explicit matching 25.12.5 SDK.

The public profile ID exposes the device family and OpenWrt version; the build strategy stays in `settings`.
