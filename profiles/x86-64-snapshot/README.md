# Generic x86/64 — OpenWrt snapshot

This profile follows OpenWrt `main` and builds the broad `full-source` package universe for generic x86/64.

The `snapshot` suffix is reserved for the real OpenWrt development snapshot. Stable release lines use `X.Y`, for example `25.12`, while exact releases use `X.Y.Z`.
